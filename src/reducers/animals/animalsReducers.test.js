import animalsReducer from './animalsReducers';
import mockAnimalsArray from '../../data/mockAnimalsArray';

describe('animalsReducers tests', () => {
  it('should return store by default', () => {

  });

  it('Should return a new store with animals', () => {
    
  })
})